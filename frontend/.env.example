# Production / preview: full API URL (no trailing slash). Leave unset in dev to use Vite proxy.
# VITE_API_BASE_URL=http://127.0.0.1:5000

# Dev only: Express server for proxying /api (match backend PORT). Default: http://127.0.0.1:5000
# VITE_DEV_PROXY_TARGET=http://127.0.0.1:5000
